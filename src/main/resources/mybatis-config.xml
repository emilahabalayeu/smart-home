<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <properties resource="config.properties"/>

    <typeAliases>
        <package name="com.solvd.smarthome.domain"/>
    </typeAliases>

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${db.driver}"/>
                <property name="url" value="${db.url}"/>
                <property name="username" value="${db.username}"/>
                <property name="password" value="${db.password}"/>
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <mapper resource="mapper/OwnerMapper.xml"/>
        <mapper resource="mapper/SmartHomeMapper.xml"/>
        <mapper resource="mapper/RoomMapper.xml"/>
        <mapper resource="mapper/DeviceMapper.xml"/>
        <mapper resource="mapper/DeviceLogMapper.xml"/>
        <mapper resource="mapper/ScheduleMapper.xml"/>
    </mappers>
</configuration>
